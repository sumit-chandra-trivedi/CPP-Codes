#include<iostream>
using namespace std;


void updateValue(int *p){
    *p = 610 % 255;
}


int main()
{
    // int i = 65;
    // char c = i;
    // cout<<c<<endl;

    // int *p = &i;
    // char *pc = (char*)p;

    // cout<<*p<<" "<<*pc<<endl;

    // char ch = 'a';
    // char* ptr = &ch;
    // cout<<ptr<<endl; // This will not print the address rather it will keep printing the characters until it finds a null character
    // //cout<<*ptr;
    // int* p = (int*) ptr;
    // cout<<p<<endl;
    // cout<<*p;

    //   char ch = 'A';
    // char *pt = &ch;
    // updateValue((int*)pt);
    // cout << ch;

   


}